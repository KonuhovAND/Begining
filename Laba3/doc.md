Вот пошаговые инструкции для выполнения задач через командную строку Linux и в VS Code.

### 🛠️ Работа в Командной строке (Linux)

В таблице ниже приведены команды для выполнения основных задач:

| Задача | Команда | Примечания |
| :--- | :--- | :--- |
| **Клонирование** | `git clone https://github.com/AllaManakhova/Python_Git_Labs.git`  | Создаст локальную папку `Python_Git_Labs` с копией репозитория. |
| **Создание ветки** | `git checkout -b feat-solve-task-one` | Флаг `-b` создает новую ветку и переключается на нее . |
| **Переключение ветки** | `git switch main` или `git checkout main` | `git switch` более современная и понятная команда . |
| **Добавление файлов** | `git add .gitignore solution_task_1.py` | Подготавливает изменения к коммиту . |
| **Создание коммита** | `git commit -m "feat: solve algorithm for task one"` | Фиксирует изменения с описанием по Conventional Commits . |
| **Отправка на сервер** | `git push -u origin feat-solve-task-one` | Флаг `-u` связывает локальную и удаленную ветку для будущих `git push` . |
| **Удаление файлов** | `git rm old_temp_file.py` | Удаляет файл из индекса и рабочей директории . |

**Создание приватного репозитория и `.gitignore`**
1.  Создайте новый репозиторий вручную на GitHub/GitLab (через веб-интерфейс), сделайте его приватным.
2.  Добавьте удаленный URL к локальному репозиторию: `git remote add new_origin <URL_вашего_приватного_репозитория>` .
3.  Создайте файл `.gitignore` в корне проекта и добавьте туда имена файлов/папок (например, `__pycache__/`, `*.log`, `.env`). Затем добавьте и запушьте его .

### 🖥️ Работа в VS Code

VS Code имеет интегрированный интерфейс для работы с Git.

1.  **Клонирование репозитория**
    *   Откройте **Command Palette** (Ctrl+Shift+P).
    *   Введите `Git: Clone` и выполните команду.
    *   Вставьте URL репозитория: `https://github.com/AllaManakhova/Python_Git_Labs.git` .
    *   Выберите папку для сохранения.

2.  **Создание и переключение веток**
    *   Нажмите на название текущей ветки (обычно в левом нижнем углу экрана).
    *   В появившемся меню выберите **Create new branch...**.
    *   Введите имя ветки (например, `feat-solve-task-one`). VS Code автоматически переключится на новую ветку .

3.  **Добавление изменений и коммит**
    *   Перейдите на вкладку **Source Control** (иконка с ветвями на левой панели).
    *   Напротив файлов, которые вы хотите включить в коммит, нажмите **+** (Stage Changes).
    *   Введите сообщение коммита в поле ввода (например, `feat: solve algorithm for task one`).
    *   Нажмите на иконку галочки для выполнения коммита .

4.  **Отправка изменений (Push)**
    *   После коммита нажмите на иконку **...** в панели Source Control.
    *   Выберите **Push** из меню. VS Code автоматически предложит создать ветку на удаленном репозитории, если её там нет.

5.  **Удаление файлов**
    *   В проводнике файлов VS Code кликните правой кнопкой мыши на файле, который нужно удалить.
    *   Выберите **Delete File...**.
    *   Удаление файла будет автоматически отображено как изменение в панели Source Control, которое нужно закоммитить .

6.  **Создание файла `.gitignore`**
    *   В проводнике VS Code кликните правой кнопкой мыши и создайте новый файл с именем `.gitignore`.
    *   Откройте его и добавьте шаблоны игнорируемых файлов, например `__pycache__/`.
    *   Сохраните файл, затем добавьте и закоммитьте его через панель Source Control, как описано выше.

### 💡 Ключевые моменты и советы

*   **Conventional Commits**: Имя каждой ветки и сообщение коммита должно соответствовать спецификации. Для задач подойдут префиксы `feat:`, `fix:`, или `docs:` .
*   **Приватный репозиторий**: Создаётся вручную на GitHub/GitLab. После этого нужно добавить его как удалённый к вашему локальному репозиторию командой `git remote add`.
*   **Содержание `.gitignore`**: Обязательно игнорируйте служебные папки Python (например, `__pycache__/`), файлы с чувствительными данными (пароли, ключи API) и временные файлы ОС.

Надеюсь, эти инструкции помогут вам успешно выполнить задание. Если возникнут трудности с какой-то конкретной командой или шагом, обращайтесь.


Отдельно создать и переключиться на ветку можно следующими способами:

## 💻 В командной строке (Linux)

### Создать ветку (без переключения)
```bash
git branch имя_ветки
```
Например:
```bash
git branch feat-solve-task-one
```

### Переключиться на существующую ветку
```bash
git checkout имя_ветки
```
Или более современная команда:
```bash
git switch имя_ветки
```

Пример последовательности:
```bash
# Создаем ветку
git branch feat-solve-task-one

# Переключаемся на нее
git switch feat-solve-task-one

# Или альтернативный вариант переключения
git checkout feat-solve-task-one
```

## 🖥️ В VS Code

### Создать ветку (без переключения)
1. Откройте **Command Palette** (Ctrl+Shift+P)
2. Введите `Git: Create Branch`
3. Введите имя ветки (например, `feat-solve-task-one`)
4. **ВАЖНО**: Когда VS Code спросит "Whether to switch to the new branch", выберите **"No"**

### Переключиться на существующую ветку
1. Нажмите на название текущей ветки в левом нижнем углу
2. В появившемся списке выберите нужную ветку из раздела **"Branches"**
3. Или используйте Command Palette → `Git: Checkout to` → выберите ветку

### Альтернативный способ в VS Code
1. Откройте панель **Source Control** (Ctrl+Shift+G)
2. В разделе **Branches** нажмите на иконку с двумя ветками
3. Выберите **Create Branch**
4. Введите имя и нажмите **Create Branch Without Checking Out**

## 🔍 Проверка текущей ветки

В любой момент можно проверить, на какой ветке вы находитесь:

**В командной строке:**
```bash
git branch
```
(звездочка * покажет текущую ветку)

**В VS Code:**
- Текущая ветка отображается в левом нижнем углу

## 💡 Практический пример

```bash
# Находимся в main
git branch

# Создаем новую ветку для задачи
git branch feat-calculator-fix

# Продолжаем работать в main...
# Позже переключаемся на созданную ветку
git switch feat-calculator-fix

# Теперь можно работать в этой ветке
```

Таким образом вы можете создавать ветки заранее, а переключаться на них когда нужно.